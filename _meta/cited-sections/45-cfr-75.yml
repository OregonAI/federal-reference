# GENERATED by src/scan_cited_sections.py -- do not hand-edit.
#
# Which sections of 45 CFR 75 get their own document, and why each one is here.
# Regenerate with:
#   python3 src/scan_cited_sections.py --erf ../executive-regulatory-frameworks --audits ../oregon-audits --title 45 --part 75
#
# Committed rather than computed during the build because CI cannot reach the sibling
# repositories this is derived from; a build-time scan would find zero and split nothing.
#
# HAND-CORRECTED (2026-09-01), all 7 entries only, moved from `removed:` to `current:`.
# The generator classifies a section by asking the LIVE eCFR version record whether it is
# removed TODAY -- correct for every other part, wrong for this one. 45 CFR 75 itself was
# removed from the CFR in its entirety effective 2025-10-01 (HHS retired its own Uniform
# Guidance for 2 CFR 200 government-wide; see this part's manifest `why`), so the live
# record marks EVERY cited section here "removed on 2025-10-01" -- including sections this
# corpus's own held snapshot already carries, because that snapshot is deliberately pinned
# to 2025-09-30, the day before removal (ADR-0001's superseded-instrument exception, the
# same date the manifest URL already pins). Running the removed-section machinery here
# would fetch a SECOND "point-in-time" snapshot at 2025-09-30 -- the exact same date -- and
# then fail, because a section cannot be both "removed" (present only in a historical
# snapshot) and present in the current part snapshot verbatim, which is exactly what
# split_cfr_sections.py's own invariant check caught. Relative to the point-in-time text
# this corpus actually holds, none of the 7 is removed, so all 7 are current here.
# `removed_on`/`name_when_in_force` are dropped since `current:` entries never carry them.

scanned_files: 82309
total_citations: 30

# Sections still in force. Text comes from the committed current part snapshot.
current:
  - section: "75.352"
    citations: 13
    cited_in: ["audits"]
  - section: "75.351"
    citations: 4
    cited_in: ["audits"]
  - section: "75.303"
    citations: 3
    cited_in: ["audits"]
  - section: "75.403"
    citations: 3
    cited_in: ["audits"]
  - section: "75.404"
    citations: 3
    cited_in: ["erf"]
  - section: "75.342"
    citations: 2
    cited_in: ["audits"]
  - section: "75.430"
    citations: 2
    cited_in: ["audits"]

# REMOVED from the CFR, but still cited by Oregon material. These carry their
# LAST-IN-FORCE text from a point-in-time snapshot, marked superseded. Resolving one
# of these to current text would answer with law that was not in force when it was
# cited -- a wrong answer wearing a right answer's clothes.
removed:

# NO eCFR version record at all -- not current, not a recorded removal. Most likely a
# pre-recodification section number eCFR's versioner never tracked (see this part's own
# SOURCE line for its recodification date) that an Oregon citation was never updated
# past. Cannot be split: there is no snapshot to cut a document from. A real citation
# pointing at nothing, named rather than silently dropped -- see ADR-0003.
unresolvable:
